# SQL Queries (Cyclistic)

This folder contains SQL queries used during the analysis.

- 01_data_checks.sql – sanity checks + filtering
- 02_feature_engineering.sql – derived columns (weekday, month, ride_length)
- 03_summary_metrics.sql – summary tables used for charts
